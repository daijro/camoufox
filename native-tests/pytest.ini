[pytest]
# This suite lives outside tests/ on purpose: tests/conftest.py belongs to the
# vendored Playwright fork and imports that suite's dependencies (pixelmatch,
# twisted, ...), which would make Camoufox's own tests unrunnable without them.
asyncio_mode = auto
markers =
    asyncio: async test, run by pytest-asyncio
addopts = -ra
