diff --git a/dom/base/ScreenDimensionManager.cpp b/dom/base/ScreenDimensionManager.cpp
new file mode 100644
index 0000000000..bcfdb0a863
--- /dev/null
+++ b/dom/base/ScreenDimensionManager.cpp
@@ -0,0 +1,45 @@
+/* Camoufox: Per-context screen dimension manager */
+#include "ScreenDimensionManager.h"
+
+#include "mozilla/dom/BrowsingContext.h"
+#include "nsGlobalWindowInner.h"
+#include "xpcpublic.h"
+
+namespace mozilla {
+namespace dom {
+
+static uint32_t UserContextIdFor(JSObject* aObj) {
+  nsGlobalWindowInner* win = xpc::WindowOrNull(aObj);
+  if (!win) return 0;
+  if (BrowsingContext* bc = win->GetBrowsingContext()) {
+    return bc->OriginAttributesRef().mUserContextId;
+  }
+  return 0;
+}
+
+static bool IsDisabled(const char* aPrefix, uint32_t aId) {
+  nsAutoString dKey;
+  dKey.AppendPrintf("%s_d_%u", aPrefix, aId);
+  bool disabled = false;
+  RoverfoxStorageManager::GetBool(dKey, disabled);
+  return disabled;
+}
+
+/* static */
+bool ScreenDimensionManager::IsDimensionsFunctionEnabledForWebIDL(
+    JSContext* aCx, JSObject* aObj) {
+  nsGlobalWindowInner* win = xpc::WindowOrNull(aObj);
+  if (!win) return false;
+  return !IsDisabled("scr_dim", UserContextIdFor(aObj));
+}
+
+/* static */
+bool ScreenDimensionManager::IsColorDepthFunctionEnabledForWebIDL(
+    JSContext* aCx, JSObject* aObj) {
+  nsGlobalWindowInner* win = xpc::WindowOrNull(aObj);
+  if (!win) return false;
+  return !IsDisabled("scr_depth", UserContextIdFor(aObj));
+}
+
+}  // namespace dom
+}  // namespace mozilla
diff --git a/dom/base/ScreenDimensionManager.h b/dom/base/ScreenDimensionManager.h
new file mode 100644
index 0000000000..6861cdcfbc
--- /dev/null
+++ b/dom/base/ScreenDimensionManager.h
@@ -0,0 +1,85 @@
+/* Camoufox: Per-context screen dimension manager */
+#ifndef mozilla_dom_ScreenDimensionManager_h
+#define mozilla_dom_ScreenDimensionManager_h
+
+#include "RoverfoxStorageManager.h"
+#include "nsString.h"
+
+namespace mozilla {
+namespace dom {
+
+// Per-context screen geometry, keyed by userContextId, stored through
+// RoverfoxStorageManager so the value survives the content-process boundary the
+// same way NavigatorManager's does.
+//
+// Width and height are stored as one pair and gated by one "disabled" flag:
+// setScreenDimensions() takes both at once, so there is no state in which only
+// one of them has been set. colorDepth is independent -- setScreenColorDepth()
+// is a separate call and self-destructs separately.
+class ScreenDimensionManager {
+ public:
+  static void SetDimensions(uint32_t aId, uint32_t aWidth, uint32_t aHeight) {
+    nsAutoString wKey;
+    wKey.AppendPrintf("scr_w_%u", aId);
+    RoverfoxStorageManager::PutUint(wKey, aWidth);
+    nsAutoString hKey;
+    hKey.AppendPrintf("scr_h_%u", aId);
+    RoverfoxStorageManager::PutUint(hKey, aHeight);
+    nsAutoString dKey;
+    dKey.AppendPrintf("scr_dim_d_%u", aId);
+    RoverfoxStorageManager::PutBool(dKey, true);
+  }
+
+  static bool GetDimensions(uint32_t aId, uint32_t& aWidth, uint32_t& aHeight) {
+    nsAutoString wKey;
+    wKey.AppendPrintf("scr_w_%u", aId);
+    nsAutoString hKey;
+    hKey.AppendPrintf("scr_h_%u", aId);
+    // Both or neither: a half-applied size is a worse lie than no lie at all,
+    // since screen.width and screen.height would come from different displays.
+    uint32_t width = 0;
+    uint32_t height = 0;
+    if (!RoverfoxStorageManager::GetUint(wKey, width) ||
+        !RoverfoxStorageManager::GetUint(hKey, height)) {
+      return false;
+    }
+    if (!width || !height) {
+      return false;
+    }
+    aWidth = width;
+    aHeight = height;
+    return true;
+  }
+
+  static void SetColorDepth(uint32_t aId, uint32_t aDepth) {
+    nsAutoString key;
+    key.AppendPrintf("scr_depth_%u", aId);
+    RoverfoxStorageManager::PutUint(key, aDepth);
+    nsAutoString dKey;
+    dKey.AppendPrintf("scr_depth_d_%u", aId);
+    RoverfoxStorageManager::PutBool(dKey, true);
+  }
+
+  static bool GetColorDepth(uint32_t aId, uint32_t& aDepth) {
+    nsAutoString key;
+    key.AppendPrintf("scr_depth_%u", aId);
+    uint32_t depth = 0;
+    if (!RoverfoxStorageManager::GetUint(key, depth) || !depth) {
+      return false;
+    }
+    aDepth = depth;
+    return true;
+  }
+
+  // WebIDL Func= gates. Each returns false once the matching setter has run,
+  // which is what removes the function from the page's view of window.
+  static bool IsDimensionsFunctionEnabledForWebIDL(JSContext* aCx,
+                                                   JSObject* aObj);
+  static bool IsColorDepthFunctionEnabledForWebIDL(JSContext* aCx,
+                                                   JSObject* aObj);
+};
+
+}  // namespace dom
+}  // namespace mozilla
+
+#endif  // mozilla_dom_ScreenDimensionManager_h
diff --git a/dom/base/moz.build b/dom/base/moz.build
index 8e94c6bd70..7607e2429e 100644
--- a/dom/base/moz.build
+++ b/dom/base/moz.build
@@ -262,6 +262,7 @@ EXPORTS.mozilla.dom += [
     "ResponsiveImageSelector.h",
     "RoverfoxStorageManager.h",
     "SameProcessMessageQueue.h",
+    "ScreenDimensionManager.h",
     "ScreenLuminance.h",
     "ScreenOrientation.h",
     "Selection.h",
@@ -551,6 +552,9 @@ SOURCES += [
 # (includes RoverfoxStorageManager.h which can affect compilation units)
 SOURCES += ["NavigatorManager.cpp"]
 
+# ScreenDimensionManager compiled separately for the same reason.
+SOURCES += ["ScreenDimensionManager.cpp"]
+
 # FontListManager compiled separately (includes RoverfoxStorageManager.h)
 SOURCES += ["FontListManager.cpp"]
 
diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 5c3d639da9..8620f732ec 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -5,6 +5,7 @@
 #include "nsGlobalWindowInner.h"
 #include "MaskConfig.hpp"
 #include "NavigatorManager.h"
+#include "ScreenDimensionManager.h"
 
 #include <inttypes.h>
 #include <stdio.h>
@@ -7921,6 +7922,56 @@ void nsGlobalWindowInner::SetNavigatorUserAgent(const nsAString& ua,
   }
 }
 
+void nsGlobalWindowInner::SetScreenDimensions(uint32_t width, uint32_t height,
+                                              ErrorResult& aRv) {
+  if (!width || !height) {
+    aRv.ThrowTypeError("Screen dimensions must be non-zero");
+    return;
+  }
+  uint32_t userContextId = 0;
+  if (BrowsingContext* bc = GetBrowsingContext()) {
+    userContextId = bc->OriginAttributesRef().mUserContextId;
+  }
+  ScreenDimensionManager::SetDimensions(userContextId, width, height);
+  if (userContextId != 0) {
+    ScreenDimensionManager::SetDimensions(0, width, height);
+  }
+  // Self-destruct: set to undefined first to bust IC/shape caches on ARM64.
+  if (JSContext* cx = nsContentUtils::GetCurrentJSContext()) {
+    JS::Rooted<JSObject*> global(cx, JS::CurrentGlobalOrNull(cx));
+    if (global) {
+      JS::Rooted<JS::Value> undef(cx, JS::UndefinedValue());
+      JS_SetProperty(cx, global, "setScreenDimensions", undef);
+      JS_DeleteProperty(cx, global, "setScreenDimensions");
+    }
+  }
+}
+
+void nsGlobalWindowInner::SetScreenColorDepth(uint32_t depth,
+                                              ErrorResult& aRv) {
+  if (!depth) {
+    aRv.ThrowTypeError("Screen colour depth must be non-zero");
+    return;
+  }
+  uint32_t userContextId = 0;
+  if (BrowsingContext* bc = GetBrowsingContext()) {
+    userContextId = bc->OriginAttributesRef().mUserContextId;
+  }
+  ScreenDimensionManager::SetColorDepth(userContextId, depth);
+  if (userContextId != 0) {
+    ScreenDimensionManager::SetColorDepth(0, depth);
+  }
+  // Self-destruct: set to undefined first to bust IC/shape caches on ARM64.
+  if (JSContext* cx = nsContentUtils::GetCurrentJSContext()) {
+    JS::Rooted<JSObject*> global(cx, JS::CurrentGlobalOrNull(cx));
+    if (global) {
+      JS::Rooted<JS::Value> undef(cx, JS::UndefinedValue());
+      JS_SetProperty(cx, global, "setScreenColorDepth", undef);
+      JS_DeleteProperty(cx, global, "setScreenColorDepth");
+    }
+  }
+}
+
 RefPtr<GenericPromise> nsGlobalWindowInner::StorageAccessPermissionChanged(
     bool aGranted) {
   // Invalidate cached StorageAllowed field so that calls to GetLocalStorage
diff --git a/dom/base/nsGlobalWindowInner.h b/dom/base/nsGlobalWindowInner.h
index 54b50f7bb1..e907a2c31a 100644
--- a/dom/base/nsGlobalWindowInner.h
+++ b/dom/base/nsGlobalWindowInner.h
@@ -701,6 +701,11 @@ class nsGlobalWindowInner final : public mozilla::dom::EventTarget,
                                        mozilla::ErrorResult& aRv);
   void SetNavigatorUserAgent(const nsAString& ua, mozilla::ErrorResult& aRv);
 
+  // Per-context screen geometry
+  void SetScreenDimensions(uint32_t width, uint32_t height,
+                           mozilla::ErrorResult& aRv);
+  void SetScreenColorDepth(uint32_t depth, mozilla::ErrorResult& aRv);
+
  public:
   void Alert(nsIPrincipal& aSubjectPrincipal, mozilla::ErrorResult& aError);
   void Alert(const nsAString& aMessage, nsIPrincipal& aSubjectPrincipal,
diff --git a/dom/base/nsScreen.cpp b/dom/base/nsScreen.cpp
index 78849001c5..d27c51aa5a 100644
--- a/dom/base/nsScreen.cpp
+++ b/dom/base/nsScreen.cpp
@@ -5,8 +5,10 @@
 #include "nsScreen.h"
 
 #include "MaskConfig.hpp"
+#include "ScreenDimensionManager.h"
 
 #include "mozilla/GeckoBindings.h"
+#include "mozilla/dom/BrowsingContext.h"
 #include "mozilla/dom/BrowsingContextBinding.h"
 #include "mozilla/dom/Document.h"
 #include "mozilla/dom/DocumentInlines.h"
@@ -23,6 +25,19 @@
 using namespace mozilla;
 using namespace mozilla::dom;
 
+// Camoufox: the userContextId this screen's window belongs to, which is the key
+// per-context spoofing is stored under. 0 is both "default context" and the
+// global fallback slot, so an unkeyed lookup still finds a value.
+static uint32_t CamouUserContextId(nsPIDOMWindowInner* aWindow) {
+  if (!aWindow) {
+    return 0;
+  }
+  if (BrowsingContext* bc = aWindow->GetBrowsingContext()) {
+    return bc->OriginAttributesRef().mUserContextId;
+  }
+  return 0;
+}
+
 nsScreen::nsScreen(nsPIDOMWindowInner* aWindow)
     : DOMEventTargetHelper(aWindow) {}
 
@@ -46,6 +61,12 @@ NS_IMPL_CYCLE_COLLECTION_INHERITED(nsScreen, DOMEventTargetHelper,
                                    mScreenOrientation)
 
 int32_t nsScreen::PixelDepth() {
+  // Camoufox: per-context colour depth wins over the global config, so
+  // contexts in one process can differ.
+  if (uint32_t depth = 0; ScreenDimensionManager::GetColorDepth(
+          CamouUserContextId(GetOwnerWindow()), depth)) {
+    return static_cast<int32_t>(depth);
+  }
   if (auto value = MaskConfig::GetUint32("screen.colorDepth"))
     return value.value();
   if (auto value = MaskConfig::GetUint32("screen.pixelDepth"))
@@ -73,6 +94,17 @@ nsDeviceContext* nsScreen::GetDeviceContext() const {
 }
 
 CSSIntRect nsScreen::GetRect() {
+  // Camoufox: per-context size wins over the global config.
+  if (uint32_t w = 0, h = 0; ScreenDimensionManager::GetDimensions(
+          CamouUserContextId(GetOwnerWindow()), w, h)) {
+    return {0, 0, static_cast<int32_t>(w), static_cast<int32_t>(h)};
+  }
+  // Camoufox: report the spoofed screen size from the injected config.
+  if (auto width = MaskConfig::GetInt32("screen.width"),
+      height = MaskConfig::GetInt32("screen.height");
+      width && height) {
+    return {0, 0, width.value(), height.value()};
+  }
   // Return window inner rect to prevent fingerprinting.
   if (ShouldResistFingerprinting(RFPTarget::ScreenRect)) {
     return GetTopWindowInnerRectForRFP();
@@ -105,6 +137,15 @@ CSSIntRect nsScreen::GetRect() {
 }
 
 CSSIntRect nsScreen::GetAvailRect() {
+  // Camoufox: the per-context API sets one size, so the available rect is the
+  // whole screen. That matches the generated fingerprints, whose availWidth /
+  // availHeight equal width / height, and keeps availWidth <= width true.
+  if (uint32_t w = 0, h = 0; ScreenDimensionManager::GetDimensions(
+          CamouUserContextId(GetOwnerWindow()), w, h)) {
+    return {0, 0, static_cast<int32_t>(w), static_cast<int32_t>(h)};
+  }
+  // Camoufox: report the spoofed available screen rect from the injected
+  // config.
   auto rect = MaskConfig::GetRect("screen.availLeft", "screen.availTop",
                                   "screen.availWidth", "screen.availHeight");
   if (rect.has_value()) {
diff --git a/dom/webidl/Window.webidl b/dom/webidl/Window.webidl
index 1aa2baa931..cde7d1ba98 100644
--- a/dom/webidl/Window.webidl
+++ b/dom/webidl/Window.webidl
@@ -985,6 +985,18 @@ partial interface Window {
   undefined setNavigatorUserAgent(DOMString ua);
 };
 
+// Per-context screen dimension spoofing
+partial interface Window {
+  [Throws, Func="mozilla::dom::ScreenDimensionManager::IsDimensionsFunctionEnabledForWebIDL"]
+  undefined setScreenDimensions(unsigned long width, unsigned long height);
+};
+
+// Per-context screen colour depth spoofing
+partial interface Window {
+  [Throws, Func="mozilla::dom::ScreenDimensionManager::IsColorDepthFunctionEnabledForWebIDL"]
+  undefined setScreenColorDepth(unsigned long depth);
+};
+
 dictionary WindowPostMessageOptions : StructuredSerializeOptions {
   USVString targetOrigin = "/";
 };
diff --git a/gfx/src/moz.build b/gfx/src/moz.build
index 91fd96a691..03adf5d674 100644
--- a/gfx/src/moz.build
+++ b/gfx/src/moz.build
@@ -95,6 +95,9 @@ if CONFIG["MOZ_WIDGET_TOOLKIT"] == "gtk":
     CXXFLAGS += CONFIG["MOZ_GTK3_CFLAGS"]
     CXXFLAGS += CONFIG["MOZ_PANGO_CFLAGS"]
 
+# DOM Mask
+LOCAL_INCLUDES += ["/camoucfg"]
+
 if CONFIG["TARGET_CPU"] == "aarch64":
     DEFINES["USE_NEON"] = True
     LOCAL_INCLUDES += ["/third_party/xsimd/include"]
diff --git a/gfx/src/nsDeviceContext.cpp b/gfx/src/nsDeviceContext.cpp
index 06b86c6c75..9641c8457c 100644
--- a/gfx/src/nsDeviceContext.cpp
+++ b/gfx/src/nsDeviceContext.cpp
@@ -3,6 +3,7 @@
  * file, You can obtain one at http://mozilla.org/MPL/2.0/. */
 
 #include "nsDeviceContext.h"
+#include "MaskConfig.hpp"
 #include <algorithm>  // for max
 #include "gfxContext.h"
 #include "gfxPoint.h"    // for gfxSize
@@ -193,6 +194,13 @@ bool nsDeviceContext::GetScreenIsHDR() {
 }
 
 nsSize nsDeviceContext::GetDeviceSurfaceDimensions() {
+  // Check for height and width overrides from MaskConfig
+  if (auto height = MaskConfig::GetInt32("screen.height"),
+      width = MaskConfig::GetInt32("screen.width");
+      height && width) {
+    nsRect outRect = CSSPixel::ToAppUnits(CSSIntRect(0, 0, width.value(), height.value()));
+    return outRect.Size();
+  }
   return GetRect().Size();
 }
 
diff --git a/layout/style/nsMediaFeatures.cpp b/layout/style/nsMediaFeatures.cpp
index 5e39014539..341c6fc853 100644
--- a/layout/style/nsMediaFeatures.cpp
+++ b/layout/style/nsMediaFeatures.cpp
@@ -4,6 +4,7 @@
 
 /* the features that media queries can test */
 
+#include "MaskConfig.hpp"
 #include "PreferenceSheet.h"
 #include "mozilla/GeckoBindings.h"
 #include "mozilla/LookAndFeel.h"
@@ -12,10 +13,12 @@
 #include "mozilla/StaticPrefs_gfx.h"
 #include "mozilla/StyleSheet.h"
 #include "mozilla/StyleSheetInlines.h"
+#include "mozilla/dom/BrowsingContext.h"
 #include "mozilla/dom/BrowsingContextBinding.h"
 #include "mozilla/dom/Document.h"
 #include "mozilla/dom/DocumentInlines.h"
 #include "mozilla/dom/ScreenBinding.h"
+#include "mozilla/dom/ScreenDimensionManager.h"
 #include "mozilla/gfx/gfxVars.h"
 #include "nsCSSProps.h"
 #include "nsCSSValue.h"
@@ -58,6 +61,32 @@ static nsSize GetSize(const Document& aDocument) {
 
 // A helper for three features below.
 static nsSize GetDeviceSize(const Document& aDocument) {
+  // Camoufox: per-context size first, for the same internal-consistency reason
+  // as the global override below -- CSS device-width has to agree with
+  // screen.width or the disagreement is itself the fingerprint.
+  if (nsPIDOMWindowInner* win = aDocument.GetInnerWindow()) {
+    uint32_t id = 0;
+    if (BrowsingContext* bc = win->GetBrowsingContext()) {
+      id = bc->OriginAttributesRef().mUserContextId;
+    }
+    if (uint32_t w = 0, h = 0;
+        ScreenDimensionManager::GetDimensions(id, w, h)) {
+      return CSSPixel::ToAppUnits(
+          CSSIntSize(static_cast<int32_t>(w), static_cast<int32_t>(h)));
+    }
+  }
+
+  // Camoufox: report the spoofed screen size for CSS device-width/height so
+  // they agree with screen.width/height (nsScreen::GetRect). This must run
+  // before the RFP early-return below, since Camoufox runs with
+  // ResistFingerprinting enabled and RFPTarget::CSSDeviceSize would otherwise
+  // return the real window size -- an internal-inconsistency lie signal.
+  if (auto width = MaskConfig::GetInt32("screen.width"),
+      height = MaskConfig::GetInt32("screen.height");
+      width && height) {
+    return CSSPixel::ToAppUnits(CSSIntSize(width.value(), height.value()));
+  }
+
   if (aDocument.ShouldResistFingerprinting(RFPTarget::CSSDeviceSize)) {
     return GetSize(aDocument);
   }
